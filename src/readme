# Source Code

This folder contains the Python scripts used for the ETL pipeline.

The scripts perform:
* Reading raw receipt files
* Extracting useful information using regular expressions
* Converting the extracted data into structured JSON format
